(function (_w, _d) {
    var CHIEF = {};

    // init
    //= init.js

    // audio
    //= audio/audio.js

    if (!_w.RadioHustleApi) {
        _w.RadioHustleApi = CHIEF;
    }
})(window, document);